{
  "kind": "collectionType",
  "collectionName": "student_invites",
  "info": {
    "name": "Student Invite"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "study": {
      "model": "study",
      "via": "student_invites"
    },
    "student": {
      "model": "student",
      "via": "student_invites"
    }
  }
}
